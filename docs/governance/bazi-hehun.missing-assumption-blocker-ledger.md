# Missing / Assumption / Blocker Ledger — BaZi Hehun

Generated: 2026-07-01T23:18:20Z
Updated: 2026-07-02 (BLOCKER-001 resolved by explicit user confirmation; council amendment D1–D8 applied: OQ-002 resolved, CONTRA-001 resolved, ASSUMPTION-003 added)
Updated: 2026-07-02 (post-audit remediation, single Phase-0.7 pass — `docs/audits/2026-07-02-bazi-hehun-spec-audit.md`: ASSUMPTION-001 verified-at-code-level; MISSING-004 launch mechanism named (CAN-015); Decisions section added — DECISION-001 plus the F1/F2 user decisions)

| ID | Marker | Item | Impact |
| --- | --- | --- | --- |
| MISSING-001 | MISSING | Complete domain-approved branch/stem interaction tables for scoring. | Blocks the deferred interaction-matrix layers and any future scoring block (D1/D3), not the raw-analysis MVP. |
| MISSING-002 | RESOLVED | Complete Ten-Gods mapping table and validation fixtures. **2026-07-04: RESOLVED.** The user provided two reference docs (`systemische_hehun_kompatibilitaetsanalyse.md`, `systematisches_handbuch_der_bazi_hehun_kompatibili.md`); the latter's Tabelle 8 gives the explicit 10x10 Shi Shen matrix, cited into `spec/rulesets/standard_bazi_2026.json` (`ten_gods` block) and implemented in `bazi_engine/match/ten_gods.py`. Validation fixtures: (1) `tests/test_match_ten_gods.py::test_ten_gods_reproduces_full_matrix` — all 100 cells reproduced; (2) `test_ten_gods_matches_worked_example_charts` — independently cross-checked against the first doc's two worked real-person charts (Person A, Person B) computed through the live engine (`time_standard="LMT"`), reproducing the source's own Ten-God assignments exactly (with one documented exception: Person A's hour pillar in the source has an internal WOZ-vs-label inconsistency — the engine reproduces the source's own math, not its mislabeled conclusion). | Ten-Gods layer itself is NOT exposed in the response schema (per D3, `FORBIDDEN_MATRIX_SUBSTRINGS` in `tests/test_match_schema.py` explicitly bars any `ten_gods`-named property — this remains correct and untouched). The table is used internally to inform `spouse_star`, which stays blocked by a NEW, distinct item — MISSING-007 (gender field). |
| MISSING-007 | RESOLVED | `spouse_star`'s convention (Tabelle 10 of the same handbook: male spouse star = Direct Wealth only, female = Direct Officer only — narrow convention, adopted verbatim, no gender-neutral "broader classical" convention assumed) needs to know each person's gender to pick the right god. `MatchRequest` (birth_input mode) carried no gender/sex field for either person. **2026-07-04: RESOLVED per docs/plans/2026-07-04-bazi-hehun-gender-field.md (GF-1..GF-6).** `MatchPersonInput(BaziRequest)` in `routers/match.py` adds an optional `gender: Literal["male","female","divers"]` field, scoped to the match endpoint only (shared `BaziRequest` used by `/calculate/bazi`/`/personalize` is untouched — regression-locked by `tests/test_match_schema.py::test_bazi_request_itself_carries_no_gender_field`). `spouse_star` moved out of `derived_fields` into its own `SpouseStarResult` type (`match/individual.py`); when gender is `male`/`female` it flips to `CALCULATED` with a located occurrence list (`match/ten_gods.py` + the ruleset `spouse_star_convention`); occurrences validated by independent re-derivation in `tests/test_match_individual_analysis.py`. | Was blocking `spouse_star` CALCULATED even with the Ten-Gods table sourced (MISSING-002). Now resolved for `male`/`female`; `divers` has its own distinct gap, MISSING-008 below (never falls back to the male/female rule). |
| MISSING-008 | MISSING | No sourced spouse-star convention exists for `divers`/non-binary gender in either reference doc (`systemische_hehun_kompatibilitaetsanalyse.md`, `systematisches_handbuch_der_bazi_hehun_kompatibili.md`) — Tabelle 10 defines only `male`→Direct Wealth and `female`→Direct Officer. | Blocks `spouse_star` from reaching `CALCULATED` when `gender="divers"` — `SpouseStarResult.__post_init__` makes a `CALCULATED` value structurally impossible for this case (`blocked_by="MISSING-008"`, empty occurrences), same discipline as every other unsourced field in this ledger. Does not block `male`/`female`. |
| MISSING-003 | MISSING | Domain-approved Day Master Strength thresholds and Yong Shen selection rules. | Blocks any future scoring block; individual DMS/Yong-Shen fields stay MISSING/NEEDS_DOMAIN_REVIEW (REQ-005). |
| MISSING-004 | USER-RESOLVED | Legal/privacy-reviewed consent copy for second-person birth data. **2026-07-04: the USER (product owner) explicitly approved the built-in UI consent text for current operation and confirmed GDPR conformance for this launch step (their own authority/risk decision, recorded verbatim — NOT an external legal opinion obtained by an agent).** Original text below. | Gates PUBLIC LAUNCH (per D5), not the build — public launch = feature-flag flip (Hehun visibility flag, DEFAULT OFF in production), gated on BOTH OQ-001 resolution AND explicit user launch sign-off (CAN-015, AC-011g/h; user decision 2026-07-02 per audit F2). MVP uses server-side-enforced consent boolean pending review (OQ-001). |
| MISSING-005 | MISSING | Runtime evidence from deployed FuFirE backend and landingpage. | Blocks Done claim. |
| ASSUMPTION-001 | ASSUMPTION — VERIFIED (code-level) | Internal BaZi calculation can be extracted into a reusable service boundary. | VERIFIED-AT-CODE-LEVEL 2026-07-02 (audit claim 1, belegt): `bazi_engine/routers/bazi.py:379` calls pure `compute_bazi()` — chart-computation reuse is satisfied. Residual for planning (audit claim 2, ableitbar): normalization equivalence (AC-003c) requires REUSING the shared response-shaping helpers (format_pillar/trace/provenance are router-embedded), not copying them. |
| ASSUMPTION-002 | ASSUMPTION | Canonical raw chart input is the most robust public raw mode. | Deferred by user decision D2 (2026-07-02): raw_bazi is cut from MVP; assumption is revisited only if/when a post-MVP raw mode is scheduled (additive, versioned). |
| ASSUMPTION-003 | ASSUMPTION | D6 demand-side success threshold: ≥10 external (non-team) calls to POST /v1/match/bazi-hehun within 30 days of landingpage visibility. | Threshold explicitly accepted by the user at canvas re-confirmation 2026-07-02 (no modification requested). Recorded as CAN-014 / EV-007; remains an assumption about demand, no longer about the threshold value. Measurement mechanism defined 2026-07-02 (audit F4, user-decided): external-call attribution via API-key allowlist — keys outside the maintained team/admin allowlist count as external; counts + key-tier only, no PII (AC-013d). ASSUMPTION-003 stays confirmed. |
| MISSING-006 | MISSING | Domain-approved Wei (未) hidden-stem ordering/weighting: ruleset `standard_bazi_2026.json` says Ji-Yi-Ding (Yi 0.5/Ding 0.3), contradicting BOTH legacy `bazi_engine/wuxing/analysis.py` AND `docs/fusion/03_wuxing_analysis.md:134` (mainstream Ji-Ding-Yi). Found by T2 divergence test 2026-07-02 (loud, not laundered). | Wei-containing charts get Wu-Xing facts disagreeing with the product's own /calculate/wuxing endpoint. Binding-source decision routed to user checkpoint 2026-07-02; final domain ruling belongs to BAZI-PRECISION-V2 domain review (same bottleneck as FBP-02-002/DEV-2026-004). |
| BLOCKER-001 | RESOLVED | User confirmation of Product Canvas and Product Vision is absent. | RESOLVED 2026-07-02: user confirmed Canvas + Vision with the exact required phrase ("Ich bestätige, dass Product Canvas und Product Vision meine Absicht korrekt wiedergeben und als Grundlage für AgileTeam Planning verwendet werden dürfen."). Note: the 2026-07-02 council amendment returned the Canvas to `draft`; re-confirmation of the amended Canvas is a new open gate (tracked in `docs/traceability.md`, not re-opened here as a laundered item). |
| OQ-002 | RESOLVED | Whether `raw_bazi` mode ships in MVP or after canonical schema review. | RESOLVED 2026-07-02 by explicit user decision (council D2, ADOPT ALL): birth_input-only MVP; `raw_bazi` / `CanonicalBaziChartInput` deferred post-MVP as an additive, versioned change. Reflected in Canvas CAN-007 and PRD REQ-002. |
| OQ-004 | RESOLVED | Privacy/consent posture for the new optional `gender` field (docs/plans/2026-07-04-bazi-hehun-gender-field.md GF-2), specifically because `divers` can reveal transgender/intersex status — more sensitive than birth data alone. **2026-07-04: RESOLVED by explicit user confirmation.** The user confirmed all three protective defaults the orchestrator applied while awaiting a response: (a) the consent-copy extension naming gender as a collected purpose is approved; (b) the strict hash-only/no-raw-echo discipline applies to `gender` — no plaintext in logs, no echo in any API response; (c) no gender-specific carve-out beyond that (person_a/person_b treated identically). | Was gating GF-3 onward at the "is this the right default" level. Now fully resolved — the user's own confirmation replaces the provisional agent default, same authority level as OQ-001/MISSING-004. |

## Decisions (post-audit remediation 2026-07-02 — all explicit user decisions)

| ID | Date | Decision | Basis |
| --- | --- | --- | --- |
| DECISION-001 | 2026-07-02 | `/v1/match/bazi-hehun` is mounted /v1-ONLY — a documented, deliberate deviation from the repo CLAUDE.md dual-mount idiom (precedent: the admin router is /v1-only). Rationale: downstream API-key consumers only call `/v1/*`. AC-001c stands unchanged. | User decision 2026-07-02; resolves audit F3. Recorded in PRD "Decisions". |
| DECISION-F1 | 2026-07-02 | The frontend repo `/Users/benjaminpoersch/Projects/SaaS/FuFirEProject/Fufire_API-landingpage` IS in scope (D8 expansion rule exercised): `src/lib/endpoint-catalog.ts` (TAG_TO_CATEGORY + category union), consent-UI component under `src/components/**`, feature-flag mechanism file(s), `public/openapi.json` + `src/api/openapi.json` snapshot sync, frontend tests. `plumbline-scope-check` runs per-repo; frontend scope is enforced procedurally via the changed-files list until tooling exists there. Council cost-saver 9 annotated FALSIFIED (evidence: `endpoint-catalog.ts:42-77`, fallback "Raw Data"). | User decision 2026-07-02; resolves audit BLOCKER F1. Recorded in the canvas "Frontend repo" subsection. |
| DECISION-F2 | 2026-07-02 | PUBLIC LAUNCH boundary = frontend feature flag: Hehun landingpage visibility ships behind a flag, DEFAULT OFF in production; flag flip = PUBLIC LAUNCH, gated on BOTH OQ-001 legal-review resolution AND explicit user launch sign-off. Backend endpoint may be live earlier (API-reachable, not advertised). | User decision 2026-07-02; resolves audit BLOCKER F2. Recorded as CAN-015 + PRD AC-011g/AC-011h. |
| DECISION-004 | 2026-07-04 | Re-authorization for the gender-field follow-up sprint (docs/plans/2026-07-04-bazi-hehun-gender-field.md): (1) OQ-004 privacy defaults confirmed as the user's own decision, not just the agent's provisional judgment (see OQ-004 row). (2) MISSING-003/006/008 stay explicitly open and honest (`PENDING_TABLES`/`NEEDS_DOMAIN_REVIEW`, confidence 0.0) — the user confirmed no fabrication is wanted; the feature is "MVP-done" with what IS sourced (Ten-Gods + male/female spouse_star), not with everything filled in. (3) Merge `feature/bazi-hehun-domain-tables` (commits `1f6d29a`/`6a0d9fe`/`e305386`) to `main` + Railway backend deploy + T22-style live smoke test authorized. (4) OQ-001/CAN-015 re-confirmed by the user for this sprint's consent-copy change (gender purpose line) — same authority as the original 2026-07-04 MISSING-004 resolution, not a new external legal opinion. (5) `VITE_FEATURE_HEHUN=true` was found ALREADY live in production (verified via `curl` against the deployed frontend BEFORE this sprint's deploy — `llms.txt` already lists `## POST /v1/match/bazi-hehun`, from the original T22-FRONTEND launch); no frontend flag action was needed, just verified, not blindly re-executed. | User decision 2026-07-04, verbatim launch/re-authorization message. Backend-only deploy for this sprint — frontend already reflects Hehun visibility from the prior launch; its `openapi.json` snapshot predates the new `gender` field (frontend tester UI won't show it until/unless the frontend repo re-syncs its bundled spec — separately scoped, not part of this authorization). |

| DECISION-002 | 2026-07-02 | Response schema for POST /v1/match/bazi-hehun follows the user draft "PartnershipAnalysisRawResponseV1" ADAPTED to the frozen spec — full binding/rejected breakdown in `docs/context/bazi-hehun.response-schema-decision.md`. D1/D3/D4 and NOGOAL-003 stay intact (no score keys, exactly 3 pair layers, no matrix/modality stubs, no LLM-readiness claim). | User decision 2026-07-02 ("Adaptiert übernehmen"). Binds Milestone-B schema task (plan T8+). |
| DECISION-003 | 2026-07-02 | Wei (未) hidden-stem weighting: match engine binds to the SAME source as /calculate/wuxing (legacy `wuxing/analysis.py`, mainstream Ji-Ding-Yi) until domain review rules on MISSING-006. Ruleset outlier stays recorded; divergence test stays loud. | User decision 2026-07-02 ("Konsistent mit /calculate/wuxing"). Resolves the T2 watcher review-required; final ruling: BAZI-PRECISION-V2 domain review. |

## Contradictions

- CONTRA-DOMAIN-TABLES-002 (2026-07-04): user directive "use these two reference docs [systemische_hehun_kompatibilitaetsanalyse.md, systematisches_handbuch_der_bazi_hehun_kompatibili.md] as tables for the calculation logic and validate with them." Findings, per table (docs/domain/bazi-hehun-domain-tables.spec.md §1-4): **ten_gods + spouse_star (§4): SOURCED** — the handbook's Tabelle 8 (10x10 Shi Shen matrix) and Tabelle 10 (spouse-star convention) are explicit, citable, and were independently cross-validated against the analysis doc's two worked charts through the live engine (`tests/test_match_ten_gods.py`); MISSING-002 resolved, `bazi_engine/match/ten_gods.py` shipped. **day_master_strength + yong_shen (§2-3): STILL UNSOURCED** — neither document contains a seasonal-weight/Wang-Xiang-Xiu-Qiu-Si table, a score formula, band cutoffs, or a Yong-Shen selection rule; the handbook only references Yong-Shen as a scoring-criterion concept, never operationalizes it. MISSING-003 stays open; no table was fabricated to close it. **day_cycle_anchor (§1): STILL UNSOURCED as a citation** — neither doc gives a JD-to-sexagenary reference with a named source; the anchor stays `unverified`. However, corroborating (non-citation-grade) evidence was found: running the actual engine (`bazi_engine.cli --standard LMT`) against the analysis doc's two real, decade-apart birth records reproduces the doc's own stated day pillars EXACTLY (1980-06-24 Hannover → Wu-Chen 戊辰; 1990-04-03 Bergisch Gladbach → Wu-Xu 戊戌), and — once the LMT time standard is used to match the doc's own True-Solar-Time methodology — ALL FOUR pillars including hour match for Person B, and year/month/day match for Person A (Person A's hour has an internal error in the source doc itself: its own computed WOZ, 14:20:27, falls in the Wei 未 13:00-15:00 bracket per the SAME doc's own hour table, yet the doc labels it Shen 申 15:00-17:00 — a civil-time bracket, not its own WOZ result; flagged, not silently corrected). This is real, useful corroboration of `DAY_OFFSET=49` / the ruleset's `anchor_jdn=2419451` — it is NOT treated as satisfying the citation requirement in the domain-tables spec (a worked example is not an authoritative reference with a named source), so `anchor_verification` stays `unverified`, consistent with CONTRA-DOMAIN-FLAGS-001's finding that birth-time precision does not verify the anchor. Also noted, out of scope (D3 locks exactly 3 MVP pair layers): the handbook contains full Hehun interaction tables (Tiangan He, Liu He/San He/San Hui, Chong/Hai/Xing, with an explicit adjacency-proximity activation rule) and a 100-point compatibility scoring matrix (Tabelle 17) that directly contradicts the ratified D1 (no fabricated score) — neither was implemented; both are available for a future, separately-ratified phase if ever wanted.

- CONTRA-DOMAIN-FLAGS-001 (2026-07-04): a post-launch "domain freigabe" asked to (a) flip DAY_ANCHOR_UNVERIFIED -> VERIFIED justified by "minute-precise birth time + coords = mathematically verified"; (b) remove the spouse-palace NEEDS_DOMAIN_REVIEW and treat it as verified; (c) add fallback (PENDING_TABLES/null) for day_master_strength/yong_shen/spouse_star "so the endpoint runs without error instead of blocking". The orchestrator did NOT implement the flag flips — they contradict the user-confirmed Vision (VIS-003/VIS-006, CAN-005/D1) and the anti-fabrication core. Findings: (a) CATEGORY ERROR — DAY_ANCHOR_UNVERIFIED tracks the ruleset day_cycle_anchor.anchor_verification (the sexagenary 60-day-cycle calibration, MISSING-006/BAZI-PRECISION-V2), which birth-time precision does not verify; flipping it asserts a domain calibration the system lacks, for ALL charts. Legit path: verify via the ruleset when domain review confirms. (b) The day-branch = spouse-palace POSITION is a standard computable fact and MAY be marked CALCULATED (identification only) with the user's confirmation; the INTERPRETATION stays deferred (no approved table). Not a blanket "verified". (c) FALSE PREMISE — the endpoint already runs without error (T22 live: HTTP 200 with honest NEEDS_DOMAIN_REVIEW/MISSING markers, confidence 0.0, DerivedFieldStatus has no value field so fabricated values are unrepresentable). Nothing blocks; the honest markers ARE the fallback. The real fields verify when the user's promised JSON mapping tables (MISSING-001..003) actually arrive. Only the user may reclassify, and doing so = the product claiming verification it lacks — surfaced for an explicit, informed decision, not silently implemented. RESOLVED 2026-07-04 (user chose the honest path): (b) spouse-palace POSITION -> CALCULATED (interpretation stays NEEDS_DOMAIN_REVIEW), (c) MISSING -> PENDING_TABLES, both live-verified on api.fufire.space (commit bd3e567); the day-anchor flip was NOT done (category error); the JSON table format to legitimately flip DMS/yong_shen/spouse_star/day_cycle_anchor is specified in docs/domain/bazi-hehun-domain-tables.spec.md, awaiting the user's domain input. Fabrication guard intact (rejects verified-looking status).

- CONTRA-LAUNCH-001 (2026-07-03): a directive arrived to flip VITE_FEATURE_HEHUN ON as the production baseline, make public/llms.txt actively promote POST /v1/match/bazi-hehun, merge both branches to main, and trigger the Railway production deploy immediately. The orchestrator did NOT execute it — it contradicts confirmed gates: (1) CAN-015 / DECISION-F2 (user-confirmed 2026-07-02): flag flip = PUBLIC LAUNCH, gated on BOTH OQ-001 legal-review resolution AND explicit user launch sign-off; (2) OQ-001 / MISSING-004 is STILL OPEN (legal/privacy review of second-person birth-data consent) — a GDPR-relevant endpoint; (3) T19 not done (TAG_TO_CATEGORY["Hehun"] unset → flag-ON currently renders Hehun under "Raw Data", violating D7); (4) T20 not done (NO consent checkbox in the frontend UI → flag-ON would publicly process second-person birth data with no UI consent control). Reversing the T18 llms.txt leak-guard + irreversible outward deploy under these open blockers is refused pending an explicit, informed user decision. Only the user may resolve; the orchestrator surfaced it verbatim (escalation-asymmetry). RESOLVED 2026-07-04 by explicit user decision (Option A): the launch directive is STOOD DOWN — the feature is finished locally (T19 category + T20 consent UI + T21 gate) with the flag STAYING OFF and the llms.txt leak-guard ACTIVE; NO deploy, NO flag flip, NO llms promotion. Public launch remains gated on OQ-001 (legal) + explicit user sign-off (CAN-015) — deferred, not done. Separately, the user explicitly CONFIRMED the mid-build evidence floor is `integration-fake` (real-boundary/integration deferred to the deploy-gated T22/MISSING-005) — this releases the T18 watcher pause.

- CONTRA-T9-SCOPE-001 (2026-07-03, Watcher pause at T9): the T9 security fix (commit ad6945a) hardened the SHARED `bazi_engine/time_utils.py` (`resolve_local_iso`) to stop interpolating raw `birth_local_iso` + `tz_name` into DST error messages that surface as 422 bodies — a genuine PII leak. VERIFIED pre-existing on `main` (not introduced here) and product-wide: `resolve_local_iso` is called by western/fusion/bazi/chart/chronometry/webhooks/personalize, all of which leaked the same PII. No existing test asserted the old message text, so the scrub breaks nothing (13 T9 + 89 regression green). BUT `time_utils.py` is NOT in the user-ratified bazi-hehun Allowed change scope and no user ratification exists (unlike T1/T7). Per escalation-asymmetry only the user resolves. Watcher option 2 (relocate scrub to match.py, revert shared) REJECTED by orchestrator analysis: it would leave the pre-existing leak in 7+ other endpoints (build-around-the-bug). RESOLVED 2026-07-03 (user decision: standalone carve-out). Part 1 (bazi-hehun `29daf78`): match.py catches LocalTimeError and re-raises a scrubbed PII-free 422 (its own T10 error contract, in-scope); time_utils.py reverted to main content (net-zero vs merge-base -> leaves the bazi-hehun branch surface; scope guard exit 0). Part 2 (WS-A `e5d4207`): standalone product-wide security commit scrubs the shared resolve_local_iso DST messages for ALL other endpoints + dedicated test tests/test_time_utils_dst_pii.py. Watcher option 2 (leave-others-leaking) rejected. No endpoint left leaking; nothing laundered.

- CONTRA-001 (artifact consistency) — **RESOLVED 2026-07-02** by `requirements-analyst`:
  - The stale intake traceability matrix v0.4 (which referenced non-existent `CAN-012`, `AC-006e`, `AC-009e`, `AC-015d`, `EV-007/EV-008`) was replaced by a rebuilt `docs/traceability.md` in which every referenced ID exists in the amended Canvas/PRD. `CAN-012`–`CAN-014` and `EV-007` now genuinely exist in the amended Canvas (D3/D5/D6); `AC-006e`/`AC-009e`/`AC-015d` are not referenced anywhere.
  - PRD AC numbering for REQ-014/REQ-015/REQ-016 was fixed: the mislabeled inner texts (`AC-NFR-001/002*` under AC-014*/AC-015*, and `AC-015a..e` under AC-016a..e) were renumbered so each criterion carries exactly its own AC ID.
  - The rebuilt matrix carries the six mandatory Canvas traceability fields and the True-Line fields (`vision-link`, `value-check-id`, `true-line-status`) for every REQ — the Phase-1 blocker condition named in the original CONTRA-001 entry is satisfied.
